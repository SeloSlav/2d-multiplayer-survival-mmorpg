// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { ContinuousSound } from "./continuous_sound_type";
import { SoundType as __SoundType } from "./sound_type_type";

import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `continuous_sound`.
 *
 * Obtain a handle from the [`continuousSound`] property on [`RemoteTables`],
 * like `ctx.db.continuousSound`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.continuousSound.on_insert(...)`.
 */
export class ContinuousSoundTableHandle {
  tableCache: TableCache<ContinuousSound>;

  constructor(tableCache: TableCache<ContinuousSound>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<ContinuousSound> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `object_id` unique index on the table `continuous_sound`,
   * which allows point queries on the field of the same name
   * via the [`ContinuousSoundObjectIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.continuousSound.object_id().find(...)`.
   *
   * Get a handle on the `object_id` unique index on the table `continuous_sound`.
   */
  object_id = {
    // Find the subscribed row whose `object_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): ContinuousSound | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.object_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: ContinuousSound) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: ContinuousSound) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: ContinuousSound) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: ContinuousSound) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: ContinuousSound, newRow: ContinuousSound) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: ContinuousSound, newRow: ContinuousSound) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
