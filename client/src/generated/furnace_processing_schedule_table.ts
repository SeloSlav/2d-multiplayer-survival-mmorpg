// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { FurnaceProcessingSchedule } from "./furnace_processing_schedule_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `furnace_processing_schedule`.
 *
 * Obtain a handle from the [`furnaceProcessingSchedule`] property on [`RemoteTables`],
 * like `ctx.db.furnaceProcessingSchedule`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.furnaceProcessingSchedule.on_insert(...)`.
 */
export class FurnaceProcessingScheduleTableHandle {
  tableCache: TableCache<FurnaceProcessingSchedule>;

  constructor(tableCache: TableCache<FurnaceProcessingSchedule>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<FurnaceProcessingSchedule> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `furnace_id` unique index on the table `furnace_processing_schedule`,
   * which allows point queries on the field of the same name
   * via the [`FurnaceProcessingScheduleFurnaceIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.furnaceProcessingSchedule.furnace_id().find(...)`.
   *
   * Get a handle on the `furnace_id` unique index on the table `furnace_processing_schedule`.
   */
  furnace_id = {
    // Find the subscribed row whose `furnace_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): FurnaceProcessingSchedule | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.furnace_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: FurnaceProcessingSchedule) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: FurnaceProcessingSchedule) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: FurnaceProcessingSchedule) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: FurnaceProcessingSchedule) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: FurnaceProcessingSchedule, newRow: FurnaceProcessingSchedule) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: FurnaceProcessingSchedule, newRow: FurnaceProcessingSchedule) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
