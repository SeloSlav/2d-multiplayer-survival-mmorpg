// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { RangedWeaponStats } from "./ranged_weapon_stats_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `ranged_weapon_stats`.
 *
 * Obtain a handle from the [`rangedWeaponStats`] property on [`RemoteTables`],
 * like `ctx.db.rangedWeaponStats`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.rangedWeaponStats.on_insert(...)`.
 */
export class RangedWeaponStatsTableHandle {
  tableCache: TableCache<RangedWeaponStats>;

  constructor(tableCache: TableCache<RangedWeaponStats>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<RangedWeaponStats> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `item_name` unique index on the table `ranged_weapon_stats`,
   * which allows point queries on the field of the same name
   * via the [`RangedWeaponStatsItemNameUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.rangedWeaponStats.item_name().find(...)`.
   *
   * Get a handle on the `item_name` unique index on the table `ranged_weapon_stats`.
   */
  item_name = {
    // Find the subscribed row whose `item_name` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): RangedWeaponStats | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.item_name, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: RangedWeaponStats) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: RangedWeaponStats) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: RangedWeaponStats) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: RangedWeaponStats) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: RangedWeaponStats, newRow: RangedWeaponStats) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: RangedWeaponStats, newRow: RangedWeaponStats) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
