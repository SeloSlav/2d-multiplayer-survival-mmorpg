// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { MemoryGridProgress } from "./memory_grid_progress_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `memory_grid_progress`.
 *
 * Obtain a handle from the [`memoryGridProgress`] property on [`RemoteTables`],
 * like `ctx.db.memoryGridProgress`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.memoryGridProgress.on_insert(...)`.
 */
export class MemoryGridProgressTableHandle {
  tableCache: TableCache<MemoryGridProgress>;

  constructor(tableCache: TableCache<MemoryGridProgress>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<MemoryGridProgress> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `player_id` unique index on the table `memory_grid_progress`,
   * which allows point queries on the field of the same name
   * via the [`MemoryGridProgressPlayerIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.memoryGridProgress.player_id().find(...)`.
   *
   * Get a handle on the `player_id` unique index on the table `memory_grid_progress`.
   */
  player_id = {
    // Find the subscribed row whose `player_id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: Identity): MemoryGridProgress | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.player_id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: MemoryGridProgress) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: MemoryGridProgress) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: MemoryGridProgress) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: MemoryGridProgress) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: MemoryGridProgress, newRow: MemoryGridProgress) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: MemoryGridProgress, newRow: MemoryGridProgress) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
